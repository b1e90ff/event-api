plugins {
    id 'java'
}

group = "de.byteterm"
version = "1.0"

repositories {
    maven {
        url = 'https://nexus.byteterm.de/repository/maven-public/'
    }
}

dependencies {
    implementation 'org.jetbrains:annotations:22.0.0'
}